# Builds self-extracting sysext installers for recent gh releases.
#
#   docker compose run --rm build
#
# Override defaults:
#   RELEASE_COUNT=3 GH_ARCH=x86_64 docker compose run --rm build

services:
  build:
    build:
      context: ../..
      dockerfile: truenas/common/Dockerfile.builder
    platform: linux/amd64
    volumes:
      - ../..:/workspace:ro
      - ./output:/output
    environment:
      RELEASE_COUNT: ${RELEASE_COUNT:-6}
      GH_ARCH: ${GH_ARCH:-x86_64}
      PLUGIN_BUILD_SCRIPT: truenas/gh/src/build.sh
      INSTALL_TEMPLATE_PATH: /workspace/truenas/gh/src/install.sh
